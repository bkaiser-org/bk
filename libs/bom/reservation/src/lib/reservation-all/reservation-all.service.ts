import { Injectable } from "@angular/core";
import { ReservationService } from "../reservation.service";

@Injectable({
  providedIn: 'root'
})
export class ReservationAllService extends ReservationService {
}